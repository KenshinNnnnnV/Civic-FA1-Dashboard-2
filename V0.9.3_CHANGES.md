# v0.9.3 real UI fix

- Fixed the actual SPORT render path (`drawReferenceSportOverlay -> drawDynamicRpmArc`).
- Neutralizes the baked tachometer scale before drawing the runtime tachometer.
- Uses elliptical geometry matching the approved 1280x720 shell instead of a circular overlay.
- Dynamic RPM rail: green 0-4000, yellow 4000-5500, red 5500-8000.
- Active ticks follow RPM; inactive ticks remain neutral.
- Removes the old top-right time and Wi-Fi cluster and moves live OBD status into that space.
- Stops the unnecessary 1 Hz clock invalidation.
- Reduces large SPORT card value typography.
- Rebuilds SELECT SENSOR with matching draw/touch geometry and hard clipping so the last row stays inside.
