# v0.9.5 test report

## Passed locally
- AndroidManifest.xml parses as XML.
- No BOOT_COMPLETED / RECEIVE_BOOT_COMPLETED autostart registration.
- MainActivity uses `singleTask` and remains in Recents.
- `onStop()` does not intentionally disconnect or shut down OBD.
- Connect / Sport / Diagnostics runtime backgrounds are all 1280x720.
- Large runtime `refPatch()` body wipes targeted by the clean-runtime pass are absent.
- Sport frame target is 16 ms (~60 FPS).
- RPM arc uses green through 4000 RPM and yellow above 4000 RPM; no red active segment.
- Sport polling priorities remain RPM 90 ms, throttle 120 ms, speed 140 ms, load 160 ms.
- Telemetry publication cap remains ~33 ms (~30 Hz).
- Java brace-balance checks pass for DashboardView, MainActivity, ObdManager, ObdProtocol, SensorFreshness.
- ObdProtocol + SensorFreshness compile with javac.
- Custom pure-Java smoke harness passed 33 parser/freshness assertions.
- GitHub Actions workflow is configured to run `:app:testDebugUnitTest` and `:app:assembleDebug`, uploading `Civic-FA1-Dashboard-v0.9.5-APK`.

## Pending on GitHub Actions / Android toolchain
The local execution environment does not contain Gradle or an Android SDK, so the full Android unit-test task and APK build must run in GitHub Actions after push. The workflow already installs Java 17, Android 34, build-tools 34.0.0, and Gradle 8.9.
