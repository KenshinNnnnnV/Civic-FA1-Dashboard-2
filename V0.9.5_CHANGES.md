# Civic FA1 Dashboard v0.9.5

- Reduced runtime Canvas repainting to dynamic values/states instead of rebuilding large card bodies.
- Preserved approved 1280x720 Connect / Sport / Diagnostics artwork as the static UI shell.
- Sport tachometer remains needle-free; dynamic RPM arc is green 0-4000 RPM and yellow above 4000 RPM.
- Static progress tracks stay in PNG artwork; runtime paints only live fill.
- Connect transport selection now changes only selection state instead of repainting the whole transport panel.
- Reduced Connect/Diagnostics clear regions to smaller dynamic areas and matched patch colors to the actual artwork.
- Kept accelerated Sport polling and ~30 Hz telemetry publication from v0.9.4.
- MainActivity stays singleTask and is now retained in Recents; minimizing does not intentionally disconnect OBD.
- No BOOT_COMPLETED receiver, startup service, WorkManager or AlarmManager autostart was added.
- Version: 0.9.5 / versionCode 26.

## Sport tachometer background fix
- Replaced runtime `background_sport.png` with the approved clean neutral-arc artwork.
- Removed the baked-in gray RPM fill/residue from the static tachometer arc.
- Dynamic RPM arc remains runtime-driven only: green up to 4000 RPM, yellow above 4000 RPM.
- No tachometer needle and no red active RPM segment.

## Final clean-runtime pass
- Removed the large per-frame `refPatch()` wipes from Connect connection status, Recent Devices, Transport Options, Connection Diagnostics, and the main Diagnostics dynamic bodies.
- Baked those dynamic regions clean into `background_connect.png` / `background_diagnostics.png` while preserving the existing frames, headers, geometry, and navigation.
- Diagnostics now draws truthful disconnected/waiting state dynamically instead of relying on baked values.
- Kept only small localized patches where a value/toggle must replace baked text; no full widget/body overlay is painted each frame.
- Repositioned readiness runtime content to match the newly-cleaned Diagnostics body.
