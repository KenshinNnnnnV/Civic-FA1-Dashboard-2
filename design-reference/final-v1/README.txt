FINAL V1 DESIGN LOCK

1) Three modes only: CONNECT / SPORT / DIAGNOSTICS.
2) Header geometry is identical in all modes; no clock or Wi-Fi icon.
3) Header accent/title differs by mode:
   CONNECT = blue/cyan, SPORT = red, DIAGNOSTICS = green.
4) Bottom navigation geometry is identical in all modes.
   Only the active tab is color-filled/glowing.
5) SPORT tachometer arc is live:
   unused RPM remains dark; active portion fills cumulatively green -> yellow -> red.
6) Target canvas: 1280x720 landscape.
