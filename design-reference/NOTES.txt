Design references for v0.4.1.
Runtime live values are NOT trusted from these static reference images.
The Android code paints the changing values from real OBD telemetry only.
When ECU is not connected, the runtime shows -- / N/A.
