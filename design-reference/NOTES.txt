These images are visual references only. v0.6.0 does NOT use their baked sensor values.
Runtime backgrounds contain only decorative city/car artwork.
The application renders gauges, cards, status, sensor values, bars and navigation as native Canvas widgets.
Target virtual canvas: 1280x720 landscape.
