These images are visual references only. v0.5.0 does NOT use their baked sensor values.
The application renders gauges, cards, status, sensor values, bars and navigation as native Canvas widgets.
Background assets are sampled only for decorative city/car artwork.
Target canvas: 1280x720 landscape.
